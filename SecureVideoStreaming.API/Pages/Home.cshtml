@page
@model SecureVideoStreaming.API.Pages.HomeModel
@{
    ViewData["Title"] = "Home - Dashboard";
}

<div class="container mt-4">
    <!-- Encabezado del Dashboard -->
    <div class="row mb-4">
        <div class="col">
            <div class="card bg-dark text-white">
                <div class="card-body">
                    <h1 class="display-5">
                        <i class="bi bi-house-door"></i> Dashboard
                    </h1>
                    <p class="lead mb-0">
                        Bienvenido, <strong>@Model.Username</strong> 
                        <span class="badge bg-primary">@Model.UserType</span>
                    </p>
                </div>
            </div>
        </div>
    </div>

    <!-- Estadísticas para Administradores -->
    @if (Model.IsAdmin)
    {
        <div class="row mb-4">
            <div class="col-md-3">
                <div class="card text-bg-primary">
                    <div class="card-body text-center">
                        <i class="bi bi-film fs-1"></i>
                        <h3 class="mt-2">@Model.TotalVideos</h3>
                        <small>Videos Propios</small>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card text-bg-success">
                    <div class="card-body text-center">
                        <i class="bi bi-key fs-1"></i>
                        <h3 class="mt-2">@Model.TotalPermisosActivos</h3>
                        <small>Permisos Activos</small>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card text-bg-info">
                    <div class="card-body text-center">
                        <i class="bi bi-hdd-stack fs-1"></i>
                        <h3 class="mt-2">@FormatFileSize(Model.AlmacenamientoUsado)</h3>
                        <small>Almacenamiento</small>
                    </div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="card text-bg-warning">
                    <div class="card-body text-center">
                        <i class="bi bi-calendar-check fs-1"></i>
                        <h3 class="mt-2">@Model.VideosSubidosHoy</h3>
                        <small>Subidos Hoy</small>
                    </div>
                </div>
            </div>
        </div>

        <!-- Acciones rápidas de administrador -->
        <div class="row mb-4">
            <div class="col-md-3">
                <a asp-page="/UploadVideo" class="btn btn-success w-100 btn-lg">
                    <i class="bi bi-cloud-upload"></i> Subir Video
                </a>
            </div>
            <div class="col-md-3">
                <a asp-page="/MyVideos" class="btn btn-primary w-100 btn-lg">
                    <i class="bi bi-collection-play"></i> Mis Videos
                </a>
            </div>
            <div class="col-md-3">
                <a asp-page="/ManagePermissions" class="btn btn-info w-100 btn-lg">
                    <i class="bi bi-shield-check"></i> Gestionar Permisos
                </a>
            </div>
            <div class="col-md-3">
                <a asp-page="/IntegrityCheck" class="btn btn-warning w-100 btn-lg">
                    <i class="bi bi-shield-exclamation"></i> Verificar Integridad
                </a>
            </div>
        </div>
    }
    else
    {
        <!-- Estadísticas para Consumidores -->
        <div class="row mb-4">
            <div class="col-md-6">
                <div class="card text-bg-success">
                    <div class="card-body text-center">
                        <i class="bi bi-unlock fs-1"></i>
                        <h3 class="mt-2">@Model.PermisosDisponibles</h3>
                        <small>Permisos Disponibles</small>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card text-bg-danger">
                    <div class="card-body text-center">
                        <i class="bi bi-lock fs-1"></i>
                        <h3 class="mt-2">@Model.PermisosExpirados</h3>
                        <small>Permisos Expirados</small>
                    </div>
                </div>
            </div>
        </div>

        <!-- Acciones rápidas de consumidor -->
        <div class="row mb-4">
            <div class="col-md-6">
                <a asp-page="/VideoGrid" class="btn btn-primary w-100 btn-lg">
                    <i class="bi bi-grid-3x3-gap"></i> Explorar Videos
                </a>
            </div>
            <div class="col-md-6">
                <a asp-page="/MyPermissions" class="btn btn-info w-100 btn-lg">
                    <i class="bi bi-key"></i> Mis Permisos
                </a>
            </div>
        </div>
    }

    <!-- Mensajes -->
    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
    {
        <div class="alert alert-danger" role="alert">
            <i class="bi bi-exclamation-triangle"></i> @Model.ErrorMessage
        </div>
    }

    @if (!string.IsNullOrEmpty(Model.SuccessMessage))
    {
        <div class="alert alert-success" role="alert">
            <i class="bi bi-check-circle"></i> @Model.SuccessMessage
        </div>
    }

    <!-- Grid de Videos -->
    <div class="row mb-4">
        <div class="col">
            <h2 class="text-white mb-3">
                <i class="bi bi-collection-play"></i> Videos Disponibles
            </h2>
        </div>
    </div>

    @if (Model.Videos == null || !Model.Videos.Any())
    {
        <div class="row">
            <div class="col">
                <div class="card">
                    <div class="card-body text-center p-5">
                        <i class="bi bi-film display-1 text-muted mb-3"></i>
                        <h3 class="text-muted">No hay videos disponibles</h3>
                        @if (Model.IsAdmin)
                        {
                            <p class="text-muted">Sube tu primer video para comenzar</p>
                            <a asp-page="/UploadVideo" class="btn btn-primary mt-3">
                                <i class="bi bi-cloud-upload"></i> Subir Video
                            </a>
                        }
                    </div>
                </div>
            </div>
        </div>
    }
    else
    {
        <div class="row row-cols-1 row-cols-md-3 g-4">
            @foreach (var video in Model.Videos)
            {
                <div class="col">
                    <div class="card video-card h-100">
                        <div class="card-body">
                            <h5 class="card-title">
                                <i class="bi bi-play-circle"></i> @video.TituloVideo
                            </h5>
                            <p class="card-text text-muted">
                                <small>
                                    <i class="bi bi-person"></i> @video.NombreAdministrador<br/>
                                    <i class="bi bi-calendar"></i> @video.FechaSubida.ToString("dd/MM/yyyy HH:mm")<br/>
                                    <i class="bi bi-hdd"></i> @FormatFileSize(video.TamañoArchivo)
                                </small>
                            </p>
                            <span class="badge bg-info">@video.EstadoProcesamiento</span>
                        </div>
                        <div class="card-footer">
                            @if (Model.IsAdmin)
                            {
                                <form method="post" asp-page-handler="DeleteVideo" asp-route-id="@video.IdVideo" class="d-inline">
                                    <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('¿Estás seguro de eliminar este video?')">
                                        <i class="bi bi-trash"></i> Eliminar
                                    </button>
                                </form>
                            }
                            else
                            {
                                <a asp-page="/VideoPlayer" asp-route-id="@video.IdVideo" class="btn btn-primary btn-sm">
                                    <i class="bi bi-play-fill"></i> Ver Video
                                </a>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>

@functions {
    string FormatFileSize(long bytes)
    {
        string[] sizes = { "B", "KB", "MB", "GB", "TB" };
        double len = bytes;
        int order = 0;
        while (len >= 1024 && order < sizes.Length - 1)
        {
            order++;
            len = len / 1024;
        }
        return $"{len:0.##} {sizes[order]}";
    }
}
